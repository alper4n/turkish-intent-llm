# Core
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
scikit-learn>=1.3
matplotlib>=3.7

# Modelling — versions are floors, not pins, so the file stays installable on
# Google Colab, whose preinstalled torch build must not be replaced.
torch>=2.1
transformers>=4.44
datasets>=2.19
accelerate>=0.30
peft>=0.11
trl>=0.9
bitsandbytes>=0.43
sentencepiece>=0.2
